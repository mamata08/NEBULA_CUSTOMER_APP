import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'NEBULA_CUSTOMER',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
